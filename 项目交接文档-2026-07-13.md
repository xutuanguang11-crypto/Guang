# 工装管家项目交接文档

更新时间：2026-07-13  
交接用途：在新的 Codex 窗口中继续维护本地工装项目管理系统。

## 1. 项目位置与启动方式

- 当前有效项目目录：
  `C:\Users\GuangTou\Documents\Codex\2026-07-12\c-users-guangtou-documents-codex-2026-2\outputs\bom-5-bom-project-bom-5-optimized`
- 默认访问地址：`http://127.0.0.1:8902`
- 数据库：`data\project_manager.db`
- 上传文件：`data\uploads`
- 推荐启动：双击项目目录中的 `双击启动系统.vbs`
- 可见窗口启动与排错：双击 `启动工装项目管理系统.cmd`
- Python 入口：`server.py`

交接时检测到 `8902` 当前没有服务监听。新窗口开始工作前，先启动系统并请求 `/api/health`，再检查页面实际加载的 JS 版本。

## 2. 技术结构

这是一个无需构建工具的本地轻量系统：

- 后端：Python 标准库 `ThreadingHTTPServer` + SQLite，核心集中在 `server.py`。
- 前端：原生 HTML、CSS、JavaScript，无 npm 构建步骤。
- 主页面：`index.html`。
- 基础前端逻辑：`app.js`。
- 主要增量模块：
  - `optimizations.js`：线索、项目、BOM、合同、发票、利润等大量页面优化。
  - `workordersv2.js`：现场工单完整流程。
  - `laborv2.js`：人力日报批量录入、审核、成本联动、编辑删除。
  - `financev4.js`：财务与利润相关增强。
  - `dashboardv5.js`：工作台经营视图。
  - `dailywork.js`、`workflow.js`、`contractflow.js`、`ledger.js` 等保留历史基础实现，后加载脚本会覆盖部分 `views.*`。
- 全局样式：`styles.css`；补充主题样式：`redwood.css`。

注意：脚本存在“后加载覆盖前实现”的结构。修改页面前，必须先确认 `index.html` 的脚本加载顺序，不要只改早期文件中的同名函数。

## 3. 已落地的主要业务能力

### 线索跟踪

- 指标卡、线索阶段漏斗、状态 Tab、负责人和日期筛选。
- 阶段进度、跟进备注、未成交标记与删除逻辑。
- 同阶段停留 3 天且没有跟进备注时，进入超时未跟进提醒。
- 负责人筛选应来自有效业务数据，避免已删除项目负责人残留。

### 项目管理

- 项目指标、负责人/状态/工期筛选、关键节点和工期预警。
- 关键节点支持新增和删除，删除后同步重算项目进度。
- 项目列表支持展开查看施工预览与节点信息。

### BOM 台账

- 材料明细、其他费用、人工费专项汇总。
- 材料及费用支持上传发票；有发票的记录使用绿色标识。
- 人力日报仅在审核通过后计入 BOM 实际人工费。
- 人工费按项目、工种、日期追溯，并联动利润看板。

### 合同、回款、发票与利润

- 合同总览、回款进度、付款节点、凭证上传和筛选。
- 发票台账支持进销项、认证/入账状态、税额口径、筛选和风险提醒。
- 税负率以已认证申报抵扣的进项税额为口径。
- 利润看板汇总合同收入、成本、回款、待开票和人工成本。

### 材料与人工价格

- 材料分类采用精简一级/二级分类与“大类缩写-序号”编码。
- 增加消防专项材料分类。
- 材料维护防火等级、集采价、零星价和标准损耗。
- 人工按大工/小工维护日单价，计件价格独立维护，不写在备注里。
- 项目专属人工价优先于全局标准人工价。

### 现场工单

- 全局工单列表、新建、搜索筛选、图片预览和详情。
- 状态流程：待派单 → 处理中 → 待验收 → 已完成；支持驳回整改。
- 责任人允许手动输入；管理员固定为系统管理员。
- 派单、整改、验收、驳回均保留操作记录。

### 人力日报

- 同一日报支持多个工种批量录入。
- 选择工种后自动带出项目专属价或全局标准日单价。
- 人数 × 天数 × 日单价自动计算人工金额。
- 支持待审核、已审核、已驳回，以及审核/驳回操作。
- 已审核日报仍可编辑和删除：
  - 编辑后自动退回“待审核”，清空原审核信息，并暂时撤销 BOM/利润成本。
  - 重新审核后按新金额再次计入成本。
  - 删除后同步从 BOM 与利润看板撤销对应人工成本。
- 最近修复的删除接口为：`DELETE /api/labor-reports/{id}`。
- 删除操作使用系统内确认弹窗，不依赖浏览器原生确认框。

## 4. 最近一次修改与验证结果

最近修改文件：

- `server.py`
  - 允许已审核日报修改并自动退回待审核。
  - 增加人力日报删除接口。
  - 更新日报时同时保存层级、班组、带队人。
  - 静态资源增加 `no-store/no-cache` 响应头，降低旧 JS 缓存问题。
- `laborv2.js`
  - 批量日报弹窗增加表头并修复输入框溢出。
  - 所有状态提供编辑/删除入口。
  - 增加已审核日报修改和删除提示。
- `styles.css`
  - 调整批量日报表格布局及删除确认样式。
- `index.html`
  - 当前 `laborv2.js` 缓存版本为 `v=5`。

已完成隔离数据库回归：

1. 新建日报。
2. 审核通过，人工费计入 BOM。
3. 修改已审核日报，状态退回待审核，BOM 实际人工费撤销。
4. 再次审核，按修改后金额重新计入。
5. 删除日报，BOM 实际人工费归零。

Python 与 JavaScript 语法检查通过。

## 5. 重要注意事项

1. **先核对运行版本**  
   页面更新但 Python 服务没有重启时，会出现“接口不存在”。每次后端修改后都必须关闭旧 Python 进程并重新启动。

2. **不要误用旧项目路径**  
   早期沟通中出现过 `2026-07-11` 目录；本交接文档第 1 节所列的 `2026-07-12\...\outputs\bom-5-bom-project-bom-5-optimized` 才是当前有效目录。

3. **保护真实数据库**  
   `data\project_manager.db` 包含用户实际业务数据。接口回归优先使用临时数据库，不要为了测试删除用户现有记录。进行结构调整前先备份数据库。

4. **避免重复定义误判**  
   多个 JS 文件可能定义同名 `views.labor`、`views.bom` 等函数。以 `index.html` 中最后加载并实际生效的定义为准。

5. **检查浏览器缓存**  
   页面源代码或浏览器脚本列表应显示 `laborv2.js?v=5`。若不是，先强制刷新；后端重启后新增的禁止缓存响应头才会生效。

6. **当前不是标准 Git 仓库工作流**  
   项目目录未检测到独立 Git 仓库。修改前后应主动记录文件范围，并保持改动小而集中。

## 6. 新窗口接手后的首轮检查

建议按以下顺序执行：

1. 进入当前有效项目目录。
2. 阅读本交接文档、`README.md`、`index.html` 的脚本加载顺序。
3. 启动 `server.py 8902`，检查 `/api/health`。
4. 确认页面加载 `optimizations.js?v=17`、`workordersv2.js?v=1`、`laborv2.js?v=5`。
5. 进入人力日报，验证删除确认弹窗与删除接口。
6. 修改后端时重启服务；修改前端时刷新并检查实际加载版本。
7. 回归时使用临时数据库，避免污染真实业务数据。

## 7. 可直接复制到新 Codex 窗口的提示词

```text
继续维护“工装管家”本地项目管理系统。

当前有效项目路径：
C:\Users\GuangTou\Documents\Codex\2026-07-12\c-users-guangtou-documents-codex-2026-2\outputs\bom-5-bom-project-bom-5-optimized

启动地址：
http://127.0.0.1:8902

请先完整阅读：
C:\Users\GuangTou\Documents\Codex\2026-07-12\c-users-guangtou-documents-codex-2026-2\outputs\bom-5-bom-project-bom-5-optimized\项目交接文档-2026-07-13.md

开始修改前先完成以下检查：
1. 确认 8902 当前运行的 Python 后端来自上述目录；
2. 请求 /api/health；
3. 确认页面实际加载 optimizations.js?v=17、workordersv2.js?v=1、laborv2.js?v=5；
4. 保护 data/project_manager.db，不要用真实数据做破坏性测试；
5. 注意多个 JS 文件存在同名 views.* 覆盖，以 index.html 中最后加载的实现为准。

最近完成的功能是人力日报：已审核日报支持编辑和删除；编辑自动退回待审核并撤销成本，删除同步撤销 BOM 和利润人工成本。删除接口是 DELETE /api/labor-reports/{id}。

请先检查当前运行状态和代码，再根据我的下一条需求做最小范围修改，并完成对应全链路回归。
```

