/* 合同变更、比例付款、竣工验收和分包质保金联动。 */
async function projectChanges(projectId){return (await api(`/api/contract-changes?project_id=${projectId}`)).items}
function changeSelect(changes){return `<select name="contract_change_id"><option value="">主合同基础项</option>${changes.map(x=>`<option value="${x.id}">${x.code} · ${x.title}</option>`).join('')}</select>`}
projectDetail=async function(id){appState.selectedProjectId=id;setHeader('projects');layout('<div class="card empty">正在加载项目详情…</div>');try{const data=await api(`/api/projects/${id}/detail`),p=data.project,c=data.contract;layout(`<div class="toolbar"><button class="secondary" id="back-projects">← 返回项目列表</button><div><button class="secondary" id="delete-project">删除项目</button>${c?'<button class="primary" id="complete-project">标记完工</button>':''}</div></div><div class="card bom-summary"><div><h3>${p.code} · ${p.name}</h3><p>项目只由赢单线索创建；完工后仍可通过总合同变更新增需求。</p></div><span class="badge ${statusClass(p.status)}">${p.status}</span></div>${!c?`<div class="card empty"><strong>尚未建立主合同</strong><p>上传主合同 PDF 后，才能设置付款节点和施工计划。</p><button class="primary" id="create-contract">建立主合同</button></div>`:`<div class="grid kpi-grid" style="margin-top:18px"><div class="card kpi"><p>合同基础金额</p><strong>${money(c.amount)}</strong></div><div class="card kpi"><p>动态合同金额</p><strong>${money(c.dynamic_amount)}</strong><span class="sub">基础合同 ± 已生效变更</span></div><div class="card kpi"><p>质保金</p><strong>${c.warranty_rate}%</strong><span class="sub">验收后 ${c.warranty_months} 个月催收</span></div></div><div class="card" style="padding:18px"><div class="form-grid"><div class="field"><label>竣工验收日期（可调整）</label><input id="acceptance-date" type="date" value="${c.acceptance_date||''}" /></div><div class="field"><label>质保金催收日期</label><input value="${data.payment_nodes.find(x=>x.name==='质保金')?.planned_date||'项目完工后自动生成'}" disabled /></div></div><div class="form-actions"><button class="secondary" id="save-acceptance">保存验收日期</button></div></div><div class="section-head"><h2>总合同变更 / 增减项</h2><button class="primary" id="add-change">＋ 新增合同变更</button></div><div class="card table-wrap"><table><thead><tr><th>变更编号</th><th>名称</th><th>类型</th><th>金额</th><th>签署日期</th><th>附件</th></tr></thead><tbody>${data.contract_changes.map(x=>`<tr><td>${x.code}</td><td>${x.title}</td><td><span class="badge ${x.change_type==='减项'?'red':'green'}">${x.change_type}</span></td><td>${money(x.amount)}</td><td>${x.signed_date||'—'}</td><td>${x.file_name||'—'}</td></tr>`).join('')||'<tr><td colspan="6" class="empty">完工后的新增需求请在此建立变更合同，再新增对应 BOM、工单和日报。</td></tr>'}</tbody></table></div><div class="section-head"><h2>施工关键节点</h2><button class="primary" id="add-milestone">＋ 新增关键节点</button></div><div class="card table-wrap"><table><thead><tr><th>节点</th><th>计划日期</th><th>进度</th><th>状态</th><th>调整</th></tr></thead><tbody>${data.milestones.map(m=>`<tr><td>${m.name}</td><td>${m.planned_date}</td><td>${m.progress}%</td><td><span class="badge ${statusClass(m.status)}">${m.status}</span></td><td><input data-milestone="${m.id}" type="range" min="0" max="100" value="${m.progress}" /><button class="action" data-save-milestone="${m.id}">保存</button></td></tr>`).join('')}</tbody></table></div><div class="section-head"><h2>甲方付款节点（按比例）</h2><button class="primary" id="add-node">＋ 添加付款节点</button></div><div class="card table-wrap"><table><thead><tr><th>节点</th><th>触发条件</th><th>比例</th><th>自动应收金额</th><th>已收</th><th>状态</th><th>操作</th></tr></thead><tbody>${data.payment_nodes.map(n=>`<tr><td>${n.name}</td><td>${data.milestones.find(m=>m.id===n.trigger_milestone_id)?.name||'合同签署后'}</td><td>${n.ratio===null?'—':n.ratio+'%'}</td><td>${money(n.effective_amount)}</td><td>${money(n.received_amount)}</td><td><span class="badge ${statusClass(n.status)}">${n.status}</span></td><td>${n.status!=='已收款'?`<button class="action" data-receipt="${n.id}">登记收款</button>`:'—'}</td></tr>`).join('')||'<tr><td colspan="7" class="empty">请按总合同金额设置付款比例，合计不能超过 100%。</td></tr>'}</tbody></table></div><div class="section-head"><h2>项目执行</h2><div><button class="secondary" id="open-bom">BOM、发票、机械费用</button> <button class="secondary" id="open-subcontracts">分包合同管理</button> <button class="primary" id="open-work">新建问题工单</button></div></div>`}`);$('#back-projects').onclick=()=>render('projects');$('#delete-project').onclick=()=>deleteProject(id);if(!c){$('#create-contract').onclick=()=>openContractModal(id);return}$('#save-acceptance').onclick=()=>saveAcceptance(c.id,id);$('#add-change').onclick=()=>openChangeModal(c.id,id);$('#complete-project').onclick=()=>completeProject(id);$('#open-bom').onclick=()=>openBom(id);$('#open-subcontracts').onclick=()=>openSubcontracts(id);$('#open-work').onclick=()=>openProjectWork(id);$('#add-milestone').onclick=()=>openMilestoneModal(id);$('#add-node').onclick=()=>openPaymentNodeModal(c.id,data.milestones,id);document.querySelectorAll('[data-save-milestone]').forEach(b=>b.onclick=async()=>{try{await api(`/api/milestones/${b.dataset.saveMilestone}/progress`,{method:'PATCH',body:JSON.stringify({progress:$(`[data-milestone="${b.dataset.saveMilestone}"]`).value})});toast('节点已更新；完成节点会触发关联收款提醒');projectDetail(id)}catch(e){toast(e.message)}});document.querySelectorAll('[data-receipt]').forEach(b=>b.onclick=()=>openReceiptModal(b.dataset.receipt,id))}catch(e){layout(`<div class="card empty">${e.message}</div>`)}};
function saveAcceptance(contractId,projectId){const dateValue=$('#acceptance-date').value;if(!dateValue)return toast('请填写竣工验收日期');api(`/api/contracts/${contractId}/acceptance`,{method:'PATCH',body:JSON.stringify({acceptance_date:dateValue})}).then(()=>{toast('竣工验收日期已保存，质保金日期已同步');projectDetail(projectId)}).catch(e=>toast(e.message))}
function openPaymentNodeModal(contractId,milestones,projectId){openModal('新增甲方付款节点',`<div class="form-grid"><div class="field"><label>付款节点名称 *</label><input name="name" required placeholder="首期款、中期款、竣工款" /></div><div class="field"><label>付款比例（%）*</label><input name="ratio" type="number" min="0.01" max="100" step="0.01" required /></div><div class="field"><label>计划收款日</label><input name="planned_date" type="date" /></div><div class="field"><label>触发关键节点</label><select name="trigger_milestone_id"><option value="">合同签署后</option>${milestones.map(m=>`<option value="${m.id}">${m.name}</option>`).join('')}</select></div></div><p class="sub">系统按动态合同金额自动计算该节点应收金额；所有比例合计不得超过 100%。</p><div class="form-actions"><button type="button" class="secondary" data-close>取消</button><button class="primary">保存节点</button></div>`,async form=>{try{const data=Object.fromEntries(new FormData(form));data.contract_id=contractId;await api('/api/payment-nodes',{method:'POST',body:JSON.stringify(data)});closeModal();projectDetail(projectId);toast('付款节点已按比例创建')}catch(e){toast(e.message)}})}
function openChangeModal(contractId,projectId){openModal('新增总合同变更',`<div class="form-grid"><div class="field full"><label>变更名称 *</label><input name="title" required placeholder="新增会议室隔断工程" /></div><div class="field"><label>变更类型 *</label><select name="change_type"><option>增项</option><option>减项</option></select></div><div class="field"><label>变更金额 *</label><input name="amount" type="number" required /></div><div class="field"><label>签署日期</label><input name="signed_date" type="date" /></div><div class="field"><label>变更合同 PDF</label><input name="change_file" type="file" accept="application/pdf" /></div></div><p class="sub">变更生效后，请在新增 BOM、工单、日报或发票时选择此变更归属。</p><div class="form-actions"><button type="button" class="secondary" data-close>取消</button><button class="primary">保存变更</button></div>`,async form=>{try{const data=Object.fromEntries(new FormData(form));Object.assign(data,await filePayload(form.elements.change_file.files[0]));delete data.change_file;data.contract_id=contractId;await api('/api/contract-changes',{method:'POST',body:JSON.stringify(data)});closeModal();projectDetail(projectId);toast('合同变更已生效，动态合同金额已更新')}catch(e){toast(e.message)}})}
openMaterialModal=async function(projectId){const changes=await projectChanges(projectId);openModal('新增 BOM 材料',`<div class="form-grid"><div class="field full"><label>合同归属</label>${changeSelect(changes)}</div><div class="field"><label>材料编码 *</label><input name="material_code" required /></div><div class="field"><label>材料名称 *</label><input name="material_name" required /></div><div class="field"><label>规格</label><input name="specification" /></div><div class="field"><label>单位 *</label><input name="unit" required /></div><div class="field"><label>设计用量 *</label><input name="design_quantity" type="number" required /></div><div class="field"><label>损耗率（%）</label><input name="loss_rate" type="number" value="0" /></div><div class="field"><label>含税单价 *</label><input name="unit_price" type="number" required /></div></div><div class="form-actions"><button type="button" class="secondary" data-close>取消</button><button class="primary">保存材料</button></div>`,async form=>{try{const data=Object.fromEntries(new FormData(form));data.project_id=projectId;await api('/api/bom-items',{method:'POST',body:JSON.stringify(data)});closeModal();openBom(projectId);toast('材料已归入对应合同层级')}catch(e){toast(e.message)}})};
openProjectWork=async function(projectId){const changes=await projectChanges(projectId);openModal('项目内新建问题工单',`<div class="form-grid"><div class="field full"><label>合同归属</label>${changeSelect(changes)}</div><div class="field full"><label>问题描述 *</label><textarea name="description" rows="3" required></textarea></div><div class="field"><label>工单类型 *</label><select name="work_type"><option>整改</option><option>一般</option></select></div><div class="field"><label>责任人 *</label><input name="assignee" required /></div><div class="field full"><label>现场图片</label><input name="photo_file" type="file" accept="image/*" /></div></div><div class="form-actions"><button type="button" class="secondary" data-close>取消</button><button class="primary">创建工单</button></div>`,async form=>{try{const data=Object.fromEntries(new FormData(form));Object.assign(data,await filePayload(form.elements.photo_file.files[0]));data.project_id=projectId;data.photo_name=data.file_name;data.photo_data=data.file_data;delete data.photo_file;await api('/api/work-orders',{method:'POST',body:JSON.stringify(data)});closeModal();projectDetail(projectId);toast('工单和现场图片已归属项目/合同层级')}catch(e){toast(e.message)}})};
openLaborModal=async function(){await loadProjects();const projectId=appState.selectedProjectId;const changes=projectId?await projectChanges(projectId):[];openModal('新建人力日报',`<div class="form-grid"><div class="field full"><label>项目 *</label>${selectProjects(projectId)}</div><div class="field full"><label>合同归属</label>${changeSelect(changes)}</div><div class="field"><label>日期 *</label><input name="report_date" type="date" required /></div><div class="field"><label>工种 *</label><input name="trade" required /></div><div class="field"><label>出勤人数 *</label><input name="workers" type="number" required /></div><div class="field"><label>工时 *</label><input name="hours" type="number" value="8" required /></div><div class="field"><label>工种单价 *</label><input name="hourly_rate" type="number" required /></div><div class="field"><label>状态 *</label><select name="status"><option>待审核</option><option>已审核</option></select></div></div><div class="form-actions"><button type="button" class="secondary" data-close>取消</button><button class="primary">保存日报</button></div>`,async form=>{try{await api('/api/labor-reports',{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(form)))});closeModal();views.labor();toast('日报已按项目和合同层级保存')}catch(e){toast(e.message)}})};
openSubNodes=async function(subcontractId,projectId){const data=await api(`/api/sub-payment-nodes?subcontract_id=${subcontractId}`);openModal('分包付款节点',`<div class="card" style="padding:0"><div class="table-wrap"><table><thead><tr><th>节点</th><th>金额</th><th>计划付款日</th><th>状态</th><th>操作</th></tr></thead><tbody>${data.items.map(x=>`<tr><td>${x.name}</td><td>${money(x.amount)}</td><td>${x.planned_date||'—'}</td><td><span class="badge ${statusClass(x.status)}">${x.status}</span></td><td>${x.status==='待付款'?`<button class="action" data-sub-paid="${x.id}">确认付款</button>`:'—'}</td></tr>`).join('')||'<tr><td colspan="5" class="empty">暂无节点</td></tr>'}</tbody></table></div></div><div class="form-actions"><button type="button" class="secondary" data-close>关闭</button><button class="primary" id="add-sub-node">新增付款节点</button></div>`,()=>{});$('#add-sub-node').onclick=()=>openSubNodeModal(subcontractId,projectId);document.querySelectorAll('[data-sub-paid]').forEach(b=>b.onclick=async()=>{try{await api(`/api/sub-payment-nodes/${b.dataset.subPaid}/paid`,{method:'PATCH',body:'{}'});closeModal();toast('已确认向分包方付款');openSubcontracts(projectId)}catch(e){toast(e.message)}})};
views.profit=async function(){try{const data=await api('/api/profit');layout(`<div class="grid kpi-grid"><div class="card kpi"><p>动态合同收入</p><strong>${money(data.items.reduce((s,x)=>s+Number(x.income),0))}</strong></div><div class="card kpi"><p>实际发票成本</p><strong>${money(data.items.reduce((s,x)=>s+Number(x.actual_cost),0))}</strong><span class="sub">材料/人工/分包发票 + 机械费用</span></div><div class="card kpi"><p>实际毛利</p><strong class="profit-positive">${money(data.items.reduce((s,x)=>s+Number(x.gross_profit),0))}</strong></div></div><div class="card table-wrap"><table><thead><tr><th>项目</th><th>动态合同收入</th><th>实际发票</th><th>机械费用</th><th>实际成本</th><th>毛利</th><th>已收款</th></tr></thead><tbody>${data.items.map(x=>`<tr><td>${x.code} · ${x.name}</td><td>${money(x.income)}</td><td>${money(x.actual_invoices)}</td><td>${money(x.equipment)}</td><td>${money(x.actual_cost)}</td><td class="profit-positive">${money(x.gross_profit)}</td><td>${money(x.received)}</td></tr>`).join('')||'<tr><td colspan="7" class="empty">暂无赢单项目</td></tr>'}</tbody></table></div><p class="sub">利润成本以实际上传并录入的发票金额为准；BOM 和人力日报仅用于预算、用量和现场管理。</p>`)}catch(e){layout('<div class="card empty">利润看板加载失败</div>')}};
openInvoicePdfModal=async function(projectId,items){const changes=await projectChanges(projectId);openModal('上传实际成本发票',`<div class="form-grid"><div class="field full"><label>合同归属</label>${changeSelect(changes)}</div><div class="field"><label>费用类型 *</label><select name="invoice_type"><option>材料</option><option>人工</option><option>分包</option></select></div><div class="field"><label>关联材料</label><select name="bom_item_id"><option value="">不关联材料</option>${items.map(x=>`<option value="${x.id}">${x.material_name}</option>`).join('')}</select></div><div class="field"><label>发票号码 *</label><input name="invoice_number" required /></div><div class="field"><label>发票金额 *</label><input name="amount" type="number" required /></div><div class="field full"><label>发票 PDF *</label><input name="invoice_file" type="file" accept="application/pdf" required /></div></div><div class="form-actions"><button type="button" class="secondary" data-close>取消</button><button class="primary">上传发票</button></div>`,async form=>{try{const data=Object.fromEntries(new FormData(form));Object.assign(data,await filePayload(form.elements.invoice_file.files[0]));data.project_id=projectId;delete data.invoice_file;await api('/api/invoices',{method:'POST',body:JSON.stringify(data)});closeModal();openBom(projectId);toast('实际发票已上传，并计入利润成本')}catch(e){toast(e.message)}})};
openEquipmentModal=async function(projectId){const changes=await projectChanges(projectId);openModal('录入机械费用',`<div class="form-grid"><div class="field full"><label>合同归属</label>${changeSelect(changes)}</div><div class="field"><label>机械/设备名称 *</label><input name="name" required /></div><div class="field"><label>费用金额 *</label><input name="amount" type="number" required /></div><div class="field"><label>发生日期 *</label><input name="cost_date" type="date" required /></div><div class="field"><label>发票 PDF *</label><input name="invoice_file" type="file" accept="application/pdf" required /></div></div><div class="form-actions"><button type="button" class="secondary" data-close>取消</button><button class="primary">保存费用</button></div>`,async form=>{try{const data=Object.fromEntries(new FormData(form));Object.assign(data,await filePayload(form.elements.invoice_file.files[0]));data.project_id=projectId;data.invoice_name=data.file_name;data.invoice_data=data.file_data;delete data.invoice_file;await api('/api/equipment-costs',{method:'POST',body:JSON.stringify(data)});closeModal();openBom(projectId);toast('机械费用和发票已计入实际成本')}catch(e){toast(e.message)}})};
render('dashboard');
