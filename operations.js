/* BOM、工单、日报的可靠操作，以及材料/人工价格字典。 */
titles.prices='材料与人工价格';
projectChanges=async function(projectId){try{return (await api(`/api/contract-changes?project_id=${projectId}`)).items||[]}catch(error){return []}};
async function optionalItems(path){try{const data=await api(path);return Array.isArray(data.items)?data.items:[]}catch(error){return []}}
views.bom=async function(){await loadProjects();const projectId=appState.selectedProjectId||appState.projects[0]?.id;if(!projectId)return layout('<div class="card empty">请先由赢单线索创建项目，再进入项目详情管理 BOM。</div>');appState.selectedProjectId=projectId;try{const bom=await api(`/api/bom-items?project_id=${projectId}`);const [invoices,equipment]=await Promise.all([optionalItems(`/api/invoices?project_id=${projectId}`),optionalItems(`/api/equipment-costs?project_id=${projectId}`)]);const materials=Array.isArray(bom.items)?bom.items:[];layout(`<div class="toolbar"><button class="secondary" id="back-detail">← 返回项目</button><div class="filters"><select id="bom-project">${appState.projects.map(p=>`<option value="${p.id}" ${p.id===projectId?'selected':''}>${p.code} · ${p.name}</option>`).join('')}</select><button class="primary" id="add-material">＋ 新增材料</button></div></div><div class="grid kpi-grid"><div class="card kpi"><p>材料预算</p><strong>${money(materials.reduce((s,x)=>s+Number(x.subtotal||0),0))}</strong></div><div class="card kpi"><p>机械费用</p><strong>${money(equipment.reduce((s,x)=>s+Number(x.amount),0))}</strong></div><div class="card kpi"><p>实际发票</p><strong>${invoices.length} 张</strong></div></div><div class="card table-wrap"><table><thead><tr><th>材料编码</th><th>材料</th><th>规格/单位</th><th>用量</th><th>单价</th><th>小计</th></tr></thead><tbody>${materials.map(x=>`<tr><td>${x.material_code}</td><td>${x.material_name}</td><td>${x.specification||'—'} / ${x.unit}</td><td>${x.design_quantity}</td><td>${money(x.unit_price)}</td><td>${money(x.subtotal||0)}</td></tr>`).join('')||'<tr><td colspan="6" class="empty">暂无材料，点击“新增材料”开始录入。</td></tr>'}</tbody></table></div><div class="section-head"><h2>实际成本附件</h2><div><button class="secondary" id="add-equipment">机械费用</button> <button class="primary" id="add-invoice">上传发票 PDF</button></div></div><div class="card table-wrap"><table><thead><tr><th>类型</th><th>名称/发票号码</th><th>金额</th><th>附件</th></tr></thead><tbody>${[...equipment.map(x=>`<tr><td>机械费用</td><td>${x.name}</td><td>${money(x.amount)}</td><td>${x.invoice_name||'—'}</td></tr>`),...invoices.map(x=>`<tr><td>${x.invoice_type}</td><td>${x.invoice_number}</td><td>${money(x.amount)}</td><td>${x.file_name||'—'}</td></tr>`)].join('')||'<tr><td colspan="4" class="empty">暂无实际成本附件</td></tr>'}</tbody></table></div>`);$('#back-detail').onclick=()=>projectDetail(projectId);$('#bom-project').onchange=e=>openBom(e.target.value);$('#add-material').onclick=()=>openMaterialModal(projectId).catch(e=>toast(e.message));$('#add-equipment').onclick=()=>openEquipmentModal(projectId).catch(e=>toast(e.message));$('#add-invoice').onclick=()=>openInvoicePdfModal(projectId,materials).catch(e=>toast(e.message))}catch(error){layout(`<div class="card empty">BOM 台账加载失败：${error.message}</div>`)}};
views.prices=async function(){try{const [materials,rates]=await Promise.all([optionalItems('/api/material-catalog'),optionalItems('/api/labor-rates')]);layout(`<div class="two-col"><div><div class="section-head"><h2>材料价格字典</h2><button class="primary" id="new-price-material">＋ 新增材料</button></div><div class="card table-wrap"><table><thead><tr><th>编码</th><th>名称</th><th>规格</th><th>单位</th><th>基准单价</th></tr></thead><tbody>${materials.map(x=>`<tr><td>${x.material_code}</td><td>${x.material_name}</td><td>${x.specification||'—'}</td><td>${x.unit}</td><td>${money(x.base_price)}</td></tr>`).join('')||'<tr><td colspan="5" class="empty">暂无材料价格</td></tr>'}</tbody></table></div></div><div><div class="section-head"><h2>人工工种价格</h2><button class="primary" id="new-price-labor">＋ 新增/更新工种</button></div><div class="card table-wrap"><table><thead><tr><th>工种</th><th>小时单价</th></tr></thead><tbody>${rates.map(x=>`<tr><td>${x.trade}</td><td>${money(x.hourly_rate)} / 小时</td></tr>`).join('')||'<tr><td colspan="2" class="empty">暂无人工单价</td></tr>'}</tbody></table></div></div></div>`);$('#new-price-material').onclick=openCatalogMaterialModal;$('#new-price-labor').onclick=openLaborRateModal}catch(e){layout('<div class="card empty">价格字典加载失败</div>')}};
function openCatalogMaterialModal(){openModal('新增材料价格',`<div class="form-grid"><div class="field"><label>材料编码 *</label><input name="material_code" required /></div><div class="field"><label>材料名称 *</label><input name="material_name" required /></div><div class="field"><label>规格</label><input name="specification" /></div><div class="field"><label>单位 *</label><input name="unit" required /></div><div class="field full"><label>基准单价 *</label><input name="base_price" type="number" required /></div></div><div class="form-actions"><button type="button" class="secondary" data-close>取消</button><button class="primary">保存材料价格</button></div>`,async form=>{try{await api('/api/material-catalog',{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(form)))});closeModal();views.prices();toast('材料价格已保存')}catch(e){toast(e.message)}})}
function openLaborRateModal(){openModal('新增/更新人工单价',`<div class="form-grid"><div class="field"><label>工种 *</label><input name="trade" required placeholder="木工、瓦工、水电" /></div><div class="field"><label>小时单价 *</label><input name="hourly_rate" type="number" required /></div></div><div class="form-actions"><button type="button" class="secondary" data-close>取消</button><button class="primary">保存单价</button></div>`,async form=>{try{await api('/api/labor-rates',{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(form)))});closeModal();views.prices();toast('人工单价已保存')}catch(e){toast(e.message)}})}
views.labor=async function(){try{await loadProjects();const reports=await optionalItems('/api/labor-reports');layout(`<div class="toolbar"><div class="filters"><select id="labor-project"><option value="">全部项目</option>${appState.projects.map(p=>`<option value="${p.id}">${p.code} · ${p.name}</option>`).join('')}</select><select id="labor-status"><option value="">全部状态</option><option>待审核</option><option>已审核</option></select></div><button class="primary" id="new-labor">＋ 新建日报</button></div><div class="card table-wrap"><table><thead><tr><th>项目</th><th>日期</th><th>工种</th><th>出勤</th><th>工时</th><th>成本</th><th>状态</th><th>操作</th></tr></thead><tbody id="labor-body"></tbody></table></div>`);const draw=()=>{const pid=$('#labor-project').value,status=$('#labor-status').value;const rows=reports.filter(x=>(!pid||String(x.project_id)===pid)&&(!status||x.status===status));$('#labor-body').innerHTML=rows.map(x=>`<tr><td>${x.project_name}</td><td>${x.report_date}</td><td>${x.trade}</td><td>${x.workers}</td><td>${x.hours}</td><td>${money(x.cost)}</td><td><span class="badge ${statusClass(x.status)}">${x.status}</span></td><td><button class="action" data-edit-report="${x.id}">编辑</button> ${x.status==='待审核'?`<button class="action" data-approve="${x.id}">审核</button>`:''} <button class="action" data-report="${x.id}">查看</button></td></tr>`).join('')||'<tr><td colspan="8" class="empty">暂无日报</td></tr>';document.querySelectorAll('[data-edit-report]').forEach(b=>b.onclick=()=>openLaborEdit(b.dataset.editReport));document.querySelectorAll('[data-approve]').forEach(b=>b.onclick=async()=>{await api(`/api/labor-reports/${b.dataset.approve}/approve`,{method:'PATCH',body:'{}'});views.labor()});document.querySelectorAll('[data-report]').forEach(b=>b.onclick=()=>openLaborReport(b.dataset.report))};$('#labor-project').onchange=draw;$('#labor-status').onchange=draw;$('#new-labor').onclick=()=>openLaborModal();draw()}catch(e){layout(`<div class="card empty">日报加载失败：${e.message}</div>`)}};
async function openLaborEdit(id){try{const x=await api(`/api/labor-reports/${id}`);openModal('编辑人力日报',`<div class="form-grid"><div class="field"><label>日期</label><input name="report_date" type="date" value="${x.report_date}" required /></div><div class="field"><label>工种</label><input name="trade" value="${x.trade}" required /></div><div class="field"><label>人数</label><input name="workers" type="number" value="${x.workers}" required /></div><div class="field"><label>工时</label><input name="hours" type="number" value="${x.hours}" required /></div><div class="field"><label>小时单价</label><input name="hourly_rate" type="number" value="${x.hourly_rate}" required /></div><div class="field"><label>状态</label><select name="status"><option ${x.status==='待审核'?'selected':''}>待审核</option><option ${x.status==='已审核'?'selected':''}>已审核</option></select></div></div><div class="form-actions"><button type="button" class="secondary" data-close>取消</button><button class="primary">保存修改</button></div>`,async form=>{try{await api(`/api/labor-reports/${id}/update`,{method:'PATCH',body:JSON.stringify(Object.fromEntries(new FormData(form)))});closeModal();views.labor();toast('日报已修改')}catch(e){toast(e.message)}})}catch(e){toast(e.message)}}
openMaterialModal=async function(projectId){const [changes,catalog]=await Promise.all([projectChanges(projectId),optionalItems('/api/material-catalog')]);openModal('新增 BOM 材料',`<div class="form-grid"><div class="field full"><label>合同归属</label>${changeSelect(changes)}</div><div class="field full"><label>从材料价格字典选择</label><select id="catalog-material"><option value="">手动填写</option>${catalog.map(x=>`<option value="${x.id}" data-code="${x.material_code}" data-name="${x.material_name}" data-spec="${x.specification||''}" data-unit="${x.unit}" data-price="${x.base_price}">${x.material_code} · ${x.material_name}</option>`).join('')}</select></div><div class="field"><label>材料编码 *</label><input name="material_code" required /></div><div class="field"><label>材料名称 *</label><input name="material_name" required /></div><div class="field"><label>规格</label><input name="specification" /></div><div class="field"><label>单位 *</label><input name="unit" required /></div><div class="field"><label>设计用量 *</label><input name="design_quantity" type="number" required /></div><div class="field"><label>损耗率（%）</label><input name="loss_rate" type="number" value="0" /></div><div class="field"><label>含税单价 *</label><input name="unit_price" type="number" required /></div></div><div class="form-actions"><button type="button" class="secondary" data-close>取消</button><button class="primary">保存材料</button></div>`,async form=>{try{const data=Object.fromEntries(new FormData(form));data.project_id=projectId;await api('/api/bom-items',{method:'POST',body:JSON.stringify(data)});closeModal();openBom(projectId);toast('材料已保存')}catch(e){toast(e.message)}});$('#catalog-material').onchange=e=>{const option=e.target.selectedOptions[0];if(!option.value)return;const fields=$('#modal-form').elements;fields.material_code.value=option.dataset.code;fields.material_name.value=option.dataset.name;fields.specification.value=option.dataset.spec;fields.unit.value=option.dataset.unit;fields.unit_price.value=option.dataset.price}}
openLaborModal=async function(){await loadProjects();const rates=await optionalItems('/api/labor-rates');const projectId=appState.selectedProjectId;const changes=projectId?await projectChanges(projectId):[];openModal('新建人力日报',`<div class="form-grid"><div class="field full"><label>项目 *</label>${selectProjects(projectId)}</div><div class="field full"><label>合同归属</label>${changeSelect(changes)}</div><div class="field"><label>日期 *</label><input name="report_date" type="date" required /></div><div class="field"><label>工种 *</label><input name="trade" list="trade-list" required /><datalist id="trade-list">${rates.map(x=>`<option value="${x.trade}"></option>`).join('')}</datalist></div><div class="field"><label>出勤人数 *</label><input name="workers" type="number" required /></div><div class="field"><label>工时 *</label><input name="hours" type="number" value="8" required /></div><div class="field"><label>小时单价 *</label><input name="hourly_rate" type="number" required /></div><div class="field"><label>状态 *</label><select name="status"><option>待审核</option><option>已审核</option></select></div></div><div class="form-actions"><button type="button" class="secondary" data-close>取消</button><button class="primary">保存日报</button></div>`,async form=>{try{await api('/api/labor-reports',{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(form)))});closeModal();views.labor();toast('日报已保存')}catch(e){toast(e.message)}});$('#modal-form').elements.trade.onchange=e=>{const rate=rates.find(x=>x.trade===e.target.value);if(rate)$('#modal-form').elements.hourly_rate.value=rate.hourly_rate}}
render('dashboard');
