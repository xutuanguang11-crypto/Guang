/* 按天/直接金额人工、价格删除，以及项目内工单闭环。 */
views.prices=async function(){try{const [materials,rates]=await Promise.all([optionalItems('/api/material-catalog'),optionalItems('/api/labor-rates')]);layout(`<div class="two-col"><div><div class="section-head"><h2>常用材料与价格</h2><button class="primary" id="new-price-material">＋ 新增材料</button></div><div class="card table-wrap"><table><thead><tr><th>编码/材料</th><th>品类/规格</th><th>供应商</th><th>单位</th><th>损耗</th><th>基准价/历史价</th><th>操作</th></tr></thead><tbody>${materials.map(x=>`<tr><td>${x.material_code}<span class="sub">${x.material_name}</span></td><td>${x.category||'—'}<span class="sub">${x.specification||'—'}</span></td><td>${x.supplier||'—'}</td><td>${x.unit}</td><td>${x.loss_rate||0}%</td><td>${money(x.base_price)}<span class="sub">历史采购 ${money(x.last_purchase_price||0)}</span></td><td><button class="action" data-delete-material="${x.id}">删除</button></td></tr>`).join('')||'<tr><td colspan="7" class="empty">暂无材料价格</td></tr>'}</tbody></table></div></div><div><div class="section-head"><h2>人工日单价</h2><button class="primary" id="new-price-labor">＋ 新增/更新工种</button></div><div class="card table-wrap"><table><thead><tr><th>工种</th><th>计价方式</th><th>日单价</th><th>操作</th></tr></thead><tbody>${rates.map(x=>`<tr><td>${x.trade}</td><td>按天</td><td>${money(x.daily_rate||0)} / 人/天</td><td><button class="action" data-delete-rate="${x.id}">删除</button></td></tr>`).join('')||'<tr><td colspan="4" class="empty">暂无人工日单价</td></tr>'}</tbody></table></div></div></div>`);$('#new-price-material').onclick=openCatalogMaterialModal;$('#new-price-labor').onclick=openLaborRateModal;document.querySelectorAll('[data-delete-material]').forEach(b=>b.onclick=()=>deletePrice(`/api/material-catalog/${b.dataset.deleteMaterial}`,'材料'));document.querySelectorAll('[data-delete-rate]').forEach(b=>b.onclick=()=>deletePrice(`/api/labor-rates/${b.dataset.deleteRate}`,'工种单价'))}catch(e){layout(`<div class="card empty">价格库加载失败：${e.message}</div>`)}};
async function deletePrice(path,label){if(!confirm(`确认删除该${label}？历史 BOM 和日报金额不会改变。`))return;try{await api(path,{method:'DELETE'});views.prices();toast(`${label}已删除`)}catch(e){toast(e.message)}}
openCatalogMaterialModal=function(){openModal('新增常用材料',`<div class="form-grid"><div class="field"><label>材料编码 *</label><input name="material_code" required /></div><div class="field"><label>材料名称 *</label><input name="material_name" required /></div><div class="field"><label>品类</label><input name="category" /></div><div class="field"><label>规格</label><input name="specification" /></div><div class="field"><label>单位 *</label><input name="unit" required /></div><div class="field"><label>默认损耗率（%）</label><input name="loss_rate" type="number" value="0" /></div><div class="field"><label>供应商</label><input name="supplier" /></div><div class="field"><label>基准单价 *</label><input name="base_price" type="number" required /></div><div class="field full"><label>历史采购价</label><input name="last_purchase_price" type="number" value="0" /></div></div><div class="form-actions"><button type="button" class="secondary" data-close>取消</button><button class="primary">保存材料</button></div>`,async form=>{try{await api('/api/material-catalog',{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(form)))});closeModal();views.prices();toast('材料价格已保存')}catch(e){toast(e.message)}})};
openLaborRateModal=function(){openModal('新增/更新人工日单价',`<div class="form-grid"><div class="field"><label>工种 *</label><input name="trade" required /></div><div class="field"><label>日单价（元/人/天）*</label><input name="daily_rate" type="number" required /></div></div><div class="form-actions"><button type="button" class="secondary" data-close>取消</button><button class="primary">保存单价</button></div>`,async form=>{try{await api('/api/labor-rates',{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(form)))});closeModal();views.prices();toast('人工日单价已保存')}catch(e){toast(e.message)}})};
openMaterialModal=async function(projectId){const [changes,catalog]=await Promise.all([projectChanges(projectId),optionalItems('/api/material-catalog')]);openModal('新增 BOM 材料',`<div class="form-grid"><div class="field full"><label>合同归属</label>${changeSelect(changes)}</div><div class="field full"><label>常用材料</label><select id="catalog-material"><option value="">手动填写</option>${catalog.map(x=>`<option value="${x.id}" data-code="${x.material_code}" data-name="${x.material_name}" data-spec="${x.specification||''}" data-unit="${x.unit}" data-price="${x.base_price}" data-loss="${x.loss_rate||0}">${x.material_code} · ${x.material_name}</option>`).join('')}</select></div><div class="field"><label>材料编码 *</label><input name="material_code" required /></div><div class="field"><label>材料名称 *</label><input name="material_name" required /></div><div class="field"><label>规格</label><input name="specification" /></div><div class="field"><label>单位 *</label><input name="unit" required /></div><div class="field"><label>设计用量 *</label><input name="design_quantity" type="number" required /></div><div class="field"><label>损耗率（%）</label><input name="loss_rate" type="number" value="0" /></div><div class="field"><label>项目单价 *</label><input name="unit_price" type="number" required /></div></div><p class="sub">损耗率和项目单价可修改；只影响当前 BOM，不修改价格库。</p><div class="form-actions"><button type="button" class="secondary" data-close>取消</button><button class="primary">保存材料</button></div>`,async form=>{try{const data=Object.fromEntries(new FormData(form));data.project_id=projectId;await api('/api/bom-items',{method:'POST',body:JSON.stringify(data)});closeModal();openBom(projectId);toast('材料已加入 BOM')}catch(e){toast(e.message)}});$('#catalog-material').onchange=e=>{const o=e.target.selectedOptions[0];if(!o.value)return;const f=$('#modal-form').elements;f.material_code.value=o.dataset.code;f.material_name.value=o.dataset.name;f.specification.value=o.dataset.spec;f.unit.value=o.dataset.unit;f.loss_rate.value=o.dataset.loss;f.unit_price.value=o.dataset.price}};
views.labor=async function(){try{await loadProjects();const reports=await optionalItems('/api/labor-reports');layout(`<div class="toolbar"><div class="filters"><select id="labor-project"><option value="">全部项目</option>${appState.projects.map(p=>`<option value="${p.id}">${p.code} · ${p.name}</option>`).join('')}</select><select id="labor-status"><option value="">全部状态</option><option>待审核</option><option>已审核</option></select></div><button class="primary" id="new-labor">＋ 新建日报</button></div><div class="card table-wrap"><table><thead><tr><th>项目</th><th>日期</th><th>工种</th><th>计价方式</th><th>人数/天数</th><th>人工金额</th><th>状态</th><th>操作</th></tr></thead><tbody id="labor-body"></tbody></table></div>`);const draw=()=>{const pid=$('#labor-project').value,status=$('#labor-status').value;const rows=reports.filter(x=>(!pid||String(x.project_id)===pid)&&(!status||x.status===status));$('#labor-body').innerHTML=rows.map(x=>`<tr><td>${x.project_name}</td><td>${x.report_date}</td><td>${x.trade}</td><td>${x.pricing_mode}</td><td>${x.pricing_mode==='直接金额'?'—':`${x.workers}人 × ${x.work_days}天`}</td><td>${money(x.cost)}</td><td><span class="badge ${statusClass(x.status)}">${x.status}</span></td><td><button class="action" data-edit-report="${x.id}">编辑</button> ${x.status==='待审核'?`<button class="action" data-approve="${x.id}">审核</button>`:''} <button class="action" data-report="${x.id}">查看</button></td></tr>`).join('')||'<tr><td colspan="8" class="empty">暂无日报</td></tr>';document.querySelectorAll('[data-edit-report]').forEach(b=>b.onclick=()=>openLaborEdit(b.dataset.editReport));document.querySelectorAll('[data-approve]').forEach(b=>b.onclick=async()=>{await api(`/api/labor-reports/${b.dataset.approve}/approve`,{method:'PATCH',body:'{}'});views.labor()});document.querySelectorAll('[data-report]').forEach(b=>b.onclick=()=>openLaborReport(b.dataset.report))};$('#labor-project').onchange=draw;$('#labor-status').onchange=draw;$('#new-labor').onclick=()=>openLaborModal();draw()}catch(e){layout(`<div class="card empty">日报加载失败：${e.message}</div>`)}};
openLaborModal=async function(){await loadProjects();const rates=await optionalItems('/api/labor-rates');const projectId=appState.selectedProjectId;const changes=projectId?await projectChanges(projectId):[];openModal('新建人力日报',laborForm({projectId,changes,rates,status:'待审核',pricing_mode:'按天',workers:1,work_days:1,daily_rate:0,direct_amount:0,report_date:'',trade:''}),async form=>{try{await api('/api/labor-reports',{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(form)))});closeModal();views.labor();toast('日报已保存')}catch(e){toast(e.message)}});bindLaborForm(rates)};
async function openLaborEdit(id){try{const [x,rates]=await Promise.all([api(`/api/labor-reports/${id}`),optionalItems('/api/labor-rates')]);openModal('编辑人力日报',laborForm({...x,rates,projectId:x.project_id,changes:[]}),async form=>{try{await api(`/api/labor-reports/${id}/update`,{method:'PATCH',body:JSON.stringify(Object.fromEntries(new FormData(form)))});closeModal();views.labor();toast('日报已修改')}catch(e){toast(e.message)}});bindLaborForm(rates)}catch(e){toast(e.message)}}
function laborForm(x){return `<div class="form-grid"><div class="field full"><label>项目 *</label>${x.project_name?`<input value="${x.project_name}" disabled />`:`${selectProjects(x.projectId)}`}</div><div class="field"><label>日期 *</label><input name="report_date" type="date" value="${x.report_date||''}" required /></div><div class="field"><label>工种 *</label><input name="trade" list="trade-list" value="${x.trade||''}" required /><datalist id="trade-list">${(x.rates||[]).map(r=>`<option value="${r.trade}"></option>`).join('')}</datalist></div><div class="field"><label>计价方式 *</label><select name="pricing_mode"><option ${x.pricing_mode==='按天'?'selected':''}>按天</option><option ${x.pricing_mode==='直接金额'?'selected':''}>直接金额</option></select></div><div class="field"><label>状态 *</label><select name="status"><option ${x.status==='待审核'?'selected':''}>待审核</option><option ${x.status==='已审核'?'selected':''}>已审核</option></select></div><div class="field labor-day"><label>人数</label><input name="workers" type="number" value="${x.workers||0}" /></div><div class="field labor-day"><label>工作天数</label><input name="work_days" type="number" step="0.5" value="${x.work_days||0}" /></div><div class="field labor-day"><label>日单价（元/人/天）</label><input name="daily_rate" type="number" value="${x.daily_rate||0}" /></div><div class="field labor-direct"><label>人工总金额</label><input name="direct_amount" type="number" value="${x.direct_amount||0}" /></div></div><div class="form-actions"><button type="button" class="secondary" data-close>取消</button><button class="primary">保存日报</button></div>`}
function bindLaborForm(rates){const form=$('#modal-form'),toggle=()=>{const direct=form.elements.pricing_mode.value==='直接金额';form.querySelectorAll('.labor-day').forEach(x=>x.classList.toggle('hidden',direct));form.querySelectorAll('.labor-direct').forEach(x=>x.classList.toggle('hidden',!direct))};form.elements.pricing_mode.onchange=toggle;form.elements.trade.onchange=e=>{const rate=rates.find(x=>x.trade===e.target.value);if(rate)form.elements.daily_rate.value=rate.daily_rate};toggle()}
const projectDetailWithoutWork=projectDetail;projectDetail=async function(id){await projectDetailWithoutWork(id);if(!$('#open-work'))return;const orders=await optionalItems(`/api/work-orders?project_id=${id}`);const section=document.createElement('div');section.innerHTML=`<div class="section-head"><h2>现场问题工单</h2></div><div class="card table-wrap"><table><thead><tr><th>编号</th><th>问题</th><th>责任人</th><th>现场图片</th><th>状态</th><th>操作</th></tr></thead><tbody>${orders.map(x=>`<tr><td>${x.code}</td><td>${x.description}</td><td>${x.assignee}</td><td>${x.photo_name||'—'}</td><td><span class="badge ${statusClass(x.status)}">${x.status}</span></td><td>${workOrderAction(x)}</td></tr>`).join('')||'<tr><td colspan="6" class="empty">暂无现场问题</td></tr>'}</tbody></table></div>`;$('#view').appendChild(section);bindWorkOrderActions(id)};
function workOrderAction(x){if(x.status==='待派发')return `<button class="action" data-work-advance="${x.id}">开始处理</button>`;if(x.status==='处理中')return `<button class="action" data-work-advance="${x.id}">提交验收</button>`;if(x.status==='待验收')return `<button class="action" data-work-close="${x.id}">验收通过</button> <button class="action" data-work-reject="${x.id}">验收不通过</button>`;return '已闭环'}
function bindWorkOrderActions(projectId){document.querySelectorAll('[data-work-advance]').forEach(b=>b.onclick=async()=>{try{await api(`/api/work-orders/${b.dataset.workAdvance}/advance`,{method:'PATCH',body:'{}'});projectDetail(projectId)}catch(e){toast(e.message)}});document.querySelectorAll('[data-work-reject]').forEach(b=>b.onclick=async()=>{try{await api(`/api/work-orders/${b.dataset.workReject}/reject`,{method:'PATCH',body:'{}'});projectDetail(projectId)}catch(e){toast(e.message)}});document.querySelectorAll('[data-work-close]').forEach(b=>b.onclick=()=>openWorkModal(b.dataset.workClose))}
openWorkModal=function(id){openModal('验收销账',`<div class="form-grid"><div class="field full"><label>销账照片 *</label><input name="photo" type="file" accept="image/*" required /></div><div class="field full"><label>验收备注</label><textarea name="note" rows="3"></textarea></div></div><div class="form-actions"><button type="button" class="secondary" data-close>取消</button><button class="primary">验收通过并销账</button></div>`,async form=>{const photo=form.elements.photo.files[0];if(!photo)return toast('请上传销账照片');try{await api(`/api/work-orders/${id}/close`,{method:'PATCH',body:JSON.stringify({closure_photo:photo.name})});closeModal();projectDetail(appState.selectedProjectId);toast('工单已销账')}catch(e){toast(e.message)}})};
openLaborReport=async function(id){try{const x=await api(`/api/labor-reports/${id}`);openModal('人力日报详情',`<div class="form-grid"><div class="field"><label>项目</label><input value="${x.project_name}" disabled /></div><div class="field"><label>日期</label><input value="${x.report_date}" disabled /></div><div class="field"><label>工种</label><input value="${x.trade}" disabled /></div><div class="field"><label>计价方式</label><input value="${x.pricing_mode}" disabled /></div><div class="field"><label>人数/天数</label><input value="${x.pricing_mode==='直接金额'?'—':`${x.workers}人 × ${x.work_days}天`}" disabled /></div><div class="field"><label>人工金额</label><input value="${money(x.cost)}" disabled /></div><div class="field full"><label>状态</label><input value="${x.status}" disabled /></div></div><div class="form-actions"><button type="button" class="primary" data-close>关闭</button></div>`,()=>{})}catch(e){toast(e.message)}};
render('dashboard');
